<nav class="navbar navbar-expand-lg navbar-light px-4 px-lg-5 py-3 py-lg-0">
    <a href="{{ path('home_page') }}" class="navbar-brand p-0">
         <img src="img/logo.png" alt="Logo">
         <h5 class="m-0">Style De Vie</h5>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
        <span class="fa fa-bars"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
        <div class="navbar-nav ms-auto py-0">
            <a href="{{ path('home_page') }}" class="nav-item nav-link active">Accueil</a>
            <a href="{{ path('contact_page') }}" class="nav-item nav-link">Contact</a>
            {% if logged %}
                <div class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">Compte</a>
                    <div class="dropdown-menu m-0">
                        <a href="{{ path('supplier_dashbord') }}" class="dropdown-item">Mon compte</a>
                        <a href="{{ path('fos_user_security_logout') }}" class="dropdown-item">Déconnexion</a>
                    </div>
                </div>
            {% endif %}
        </div>
        {% if not logged %}
            <a href="{{ path('supplier_dashbord') }}" class="btn btn-light rounded-pill text-primary py-2 px-4 ms-lg-5">Login</a>
            <a href="{{ path('become_supplier') }}" class="btn btn-light rounded-pill text-primary py-2 px-4 ms-lg-2">Devenir Fournisseur</a>
        {% endif %}
    </div>
</nav>