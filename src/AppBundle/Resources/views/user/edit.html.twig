{% extends '@App/base.html.twig' %}

{% import "@SbSAdminLTE/Layout/main_macros.html.twig" as macros %}

{% block page_header %}
    {{ macros.page_header("Administrateurs", "Modifier") }}
{% endblock %}

{% block page_content %}

<div class="container-fluid">
  <div class="row">
    <!-- /.col -->
    <div class="col-md-12">
      <div class="card">
        <div class="card-header p-2">
          <strong>Modifier l'administrateur </strong>
        </div><!-- /.card-header -->
        <div class="card-body">
          <div class="tab-content">
            {{ form_start(edit_form) }}
                <div class="form-group row">
                  <label for="email" class="col-sm-2 col-form-label">{{ form_label(edit_form.firstName) }}</label>
                  <div class="col-sm-10">
                    {{ form_widget(edit_form.firstName) }}
                    <div class="help-block with-errors" style="font-weight:bold;color:#ff0000;">{{ form_errors(edit_form.firstName) }}</div>
                  </div>
                </div>
                <div class="form-group row">
                  <label for="email" class="col-sm-2 col-form-label">{{ form_label(edit_form.lastName) }}</label>
                  <div class="col-sm-10">
                    {{ form_widget(edit_form.lastName) }}
                    <div class="help-block with-errors" style="font-weight:bold;color:#ff0000;">{{ form_errors(edit_form.lastName) }}</div>
                  </div>
                </div>
                <div class="form-group row">
                  <label for="email" class="col-sm-2 col-form-label">{{ form_label(edit_form.phone) }}</label>
                  <div class="col-sm-10">
                    {{ form_widget(edit_form.phone) }}
                    <div class="help-block with-errors" style="font-weight:bold;color:#ff0000;">{{ form_errors(edit_form.phone) }}</div>
                  </div>
                </div>
                <div class="form-group row">
                  <label for="email" class="col-sm-2 col-form-label">{{ form_label(edit_form.cin) }}</label>
                  <div class="col-sm-10">
                    {{ form_widget(edit_form.cin) }}
                    <div class="help-block with-errors" style="font-weight:bold;color:#ff0000;">{{ form_errors(edit_form.cin) }}</div>
                  </div>
                </div>
                <div class="form-group row">
                  <label for="username" class="col-sm-2 col-form-label">{{ form_label(edit_form.username) }}</label>
                  <div class="col-sm-10">
                    {{ form_widget(edit_form.username) }}
                    <div class="help-block with-errors" style="font-weight:bold;color:#ff0000;">{{ form_errors(edit_form.username) }}</div>
                  </div>
                </div>
                <div class="form-group row">
                  <label for="email" class="col-sm-2 col-form-label">{{ form_label(edit_form.email) }}</label>
                  <div class="col-sm-10">
                    {{ form_widget(edit_form.email) }}
                    <div class="help-block with-errors" style="font-weight:bold;color:#ff0000;">{{ form_errors(edit_form.email) }}</div>
                  </div>
                </div>
                <div class="form-group row">
                  <label for="email" class="col-sm-2 col-form-label">{{ form_label(edit_form.enabled) }}</label>
                  <div class="col-sm-10">
                    {{ form_widget(edit_form.enabled) }}
                  </div>
                </div>

                <div class="form-group row">
                  <label for="email" class="col-sm-2 col-form-label">{{ form_label(edit_form.plainPassword.first) }}</label>
                  <div class="col-sm-10">
                    {{ form_widget(edit_form.plainPassword.first) }}
                    <div class="help-block with-errors" style="font-weight:bold;color:#ff0000;">{{ form_errors(edit_form.plainPassword.first) }}</div>
                  </div>
                </div>
                <div class="form-group row">
                  <label for="email" class="col-sm-2 col-form-label">{{ form_label(edit_form.plainPassword.second) }}</label>
                  <div class="col-sm-10">
                    {{ form_widget(edit_form.plainPassword.second) }}
                    <div class="help-block with-errors" style="font-weight:bold;color:#ff0000;">{{ form_errors(edit_form.plainPassword.second) }}</div>
                  </div>
                </div>


                <div class="form-group row">
                  <div class="offset-sm-2 col-sm-10">
                    <button type="submit" class="btn btn-danger">Submit</button>
                  </div>
                </div>

            {{ form_end(edit_form) }}
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
{% endblock %}

{% block javascripts_surcharge %}
<script>
$(function () {
  //Initialize Select2 Elements
  $('.select2').select2()

  //Initialize Select2 Elements
  $('.select2bs4').select2({
    theme: 'bootstrap4'
  })
})
</script>
{% endblock %}
